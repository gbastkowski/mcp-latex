% Shared by every preset: glyph fallbacks and the table-wrap fix. Layout- and
% type-independent. Concatenated first, before the layout and type partials.
% Placeholder __LINK_COLOR__ is substituted by the MCP server.
\usepackage{xcolor}
\definecolor{Blue}{HTML}{__LINK_COLOR__} % link colour (layouts may redefine)

% Kill common ligatures in the monospace font. Without this, a `--` inside a
% code block is typeset as an en-dash: `mcp-latex--render` shows as
% `mcp-latex–render` and `--pdf-engine` as `–pdf-engine`, so copying code out of
% the PDF yields the wrong text. Prose keeps its normal en/em-dash ligatures —
% this applies only to \ttfamily.
\usepackage{fontspec}
\defaultfontfeatures[\ttfamily]{Ligatures=NoCommon}

% Long monospace identifiers (e.g. http_server_requests_seconds_count) are a
% single unbreakable \texttt token and overrun narrow table columns. Redefine
% \texttt so it allows a line break after each `_`, `.`, `/`, `-` or `:` — the
% content is unchanged, it just wraps instead of colliding with the next cell.
%
% The wrapper is a SEPARATE robust command that \texttt delegates to, rather than
% \texttt being redefined in place. A heading containing code lands in a moving
% argument — the TOC entry, the running head, a PDF bookmark — and the fragile
% \def\_ inside a plain \renewcommand fails there with "Use of \protect doesn't
% match its definition" (issue #4). It only bit the types that write chapter marks
% as well as TOC entries, `reference` and `komabook`, so a plain report hid it.
%
% \origtexttt has to be captured before any of this: declaring \texttt itself
% robust makes it expand via \texttt␣, so the capture would point at the wrapper
% and recurse until "TeX capacity exceeded".
\let\origtexttt\texttt
\DeclareRobustCommand{\wraptt}[1]{{%
  \def\_{\textunderscore\allowbreak}%
  \origtexttt{#1}%
}}
\renewcommand{\texttt}[1]{\wraptt{#1}}

% The body fonts (Palatino / Latin Modern) have no glyphs for arrows or emoji,
% which otherwise render as empty boxes. Map the common ones to text/math
% equivalents so they at least carry their meaning.
\usepackage{newunicodechar}
% Arrows.
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{←}{\ensuremath{\leftarrow}}
\newunicodechar{↔}{\ensuremath{\leftrightarrow}}
\newunicodechar{⇒}{\ensuremath{\Rightarrow}}
\newunicodechar{⇐}{\ensuremath{\Leftarrow}}
% Status emoji → coloured symbols.
\newunicodechar{✅}{\textcolor[HTML]{2E7D32}{\ensuremath{\checkmark}}}
\newunicodechar{✔}{\textcolor[HTML]{2E7D32}{\ensuremath{\checkmark}}}
\newunicodechar{☑}{\textcolor[HTML]{2E7D32}{\ensuremath{\checkmark}}}
\newunicodechar{❌}{\textcolor[HTML]{C62828}{\ensuremath{\times}}}
\newunicodechar{✖}{\textcolor[HTML]{C62828}{\ensuremath{\times}}}
\newunicodechar{✗}{\textcolor[HTML]{C62828}{\ensuremath{\times}}}
\newunicodechar{⚠}{\textbf{[!]}}
\newunicodechar{ℹ}{\textbf{[i]}}
% Bullets / misc.
\newunicodechar{•}{\ensuremath{\bullet}}
\newunicodechar{★}{\ensuremath{\star}}
\newunicodechar{☆}{\ensuremath{\star}}
